#include<stdio.h>
int main(){
int c,d;
printf("Enter the value you want:");
scanf("%d",&c);
printf("Enter the value you want:");
scanf("%d",&d);
if(c>d){
    printf("c is bigger than d");
}
    else{
        printf("d is bigger than c");
    }

getch();
}
